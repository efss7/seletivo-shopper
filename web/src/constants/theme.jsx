import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#52B591',
      contrastText: 'white',
    },
    neutralColor: {
      main: '#1D1A39',
      contrastText: 'white',
    },
  },
});

export default theme;
